fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
sqlalchemy==2.0.23
asyncpg==0.29.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
python-dateutil>=2.8.0

# Keep Qlib isolated from the backend runtime. The bootstrap scorer works without
# pyqlib; production Qlib experiments should add pyqlib in this image once the
# concrete qrun config is promoted.
